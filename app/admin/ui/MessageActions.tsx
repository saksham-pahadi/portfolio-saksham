"use client"; import { useRouter } from "next/navigation";
export default function MessageActions({id,status}:{id:string;status:string}){const r=useRouter();async function patch(next:string){await fetch(`/api/admin/messages/${id}`,{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({status:next})});r.refresh();} return <div className="flex gap-2"><button onClick={()=>patch(status==="new"?"read":"new")} className="rounded-full border border-white/[.08] px-3 py-1.5 text-[10px] text-white/[.45]">{status==="new"?"Mark read":"Unread"}</button><button onClick={()=>patch("replied")} className="rounded-full border border-[#d6ff3f]/15 px-3 py-1.5 text-[10px] text-[#dfff83]">Replied</button></div>}
