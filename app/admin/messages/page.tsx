import { requireAdmin } from "@/lib/require-admin";
import { connectDB } from "@/lib/mongodb"; import Message from "@/models/Message"; import MessageActions from "@/app/admin/ui/MessageActions"; import { formatDate } from "@/lib/utils";
export const dynamic="force-dynamic";
export default async function Messages(){await requireAdmin();await connectDB();const docs=await Message.find().select("-ipHash").sort({createdAt:-1}).limit(100).lean();return <div className="px-5 py-8 md:px-8"><p className="mono text-xs uppercase tracking-[.25em] text-[#9b5cff]">inbox / contact</p><h1 className="mt-2 text-3xl font-bold">Messages</h1><div className="mt-8 grid gap-4">{docs.length===0?<div className="rounded-2xl border border-white/[.07] bg-[#111017] p-8 text-sm text-white/[.35]">Your inbox is clean.</div>:docs.map((m:any)=><div key={String(m._id)} className="rounded-[24px] border border-white/[.07] bg-[#111017] p-6"><div className="flex flex-col justify-between gap-3 md:flex-row"><div><p className="text-lg font-semibold">{m.subject}</p><p className="mt-1 text-xs text-white/[.35]">{m.name} · {m.email} · {formatDate(m.createdAt)}</p></div><MessageActions id={String(m._id)} status={m.status}/></div><p className="mt-5 whitespace-pre-wrap text-sm leading-7 text-white/[.55]">{m.message}</p></div>)}</div></div>}
