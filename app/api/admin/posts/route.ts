import { NextResponse } from "next/server"; import { auth } from "@/auth"; import { connectDB } from "@/lib/mongodb"; import Post from "@/models/Post"; import { slugify } from "@/lib/utils";
export async function GET(){const s=await auth();if(!s?.user)return NextResponse.json({error:"Unauthorized"},{status:401});await connectDB();const docs=await Post.find().sort({publishedAt:-1}).lean();return NextResponse.json(docs.map(d=>({...d,_id:String(d._id)})));}
export async function POST(req:Request){const s=await auth();if(!s?.user)return NextResponse.json({error:"Unauthorized"},{status:401});await connectDB();const b=await req.json();const doc=await Post.create({...b,slug:slugify(b.slug||b.title),publishedAt:b.publishedAt||new Date()});return NextResponse.json({_id:String(doc._id)},{status:201});}
